<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
       x:Class="Splitt.Views.NewCheckItemView"
       xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:vm="clr-namespace:Splitt.ViewModels"
       x:DataType="vm:NewCheckItemViewModel">

       <ContentPage.BindingContext>
              <vm:NewCheckItemViewModel/>
       </ContentPage.BindingContext>

       <VerticalStackLayout Padding="20"
                            MaximumWidthRequest="600">
              <!-- <VerticalStackLayout>
                     <Label Text="Title"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Title}"/>
                     <Label Text="Owner ID"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding OwnerId}"/>
                     <Label Text="Date"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Date}"/>
                     <Label Text="Subtotal"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Subtotal}"/>
                     <Label Text="Tax"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Tax}"/>
                     <Label Text="Tip"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Tip}"/>
                     <Label Text="Total"
                            Margin="0,10,0,0"/>
                     <Entry Margin="0,2,0,0"
                            Text="{Binding Total}"/>
              </VerticalStackLayout> -->

              <!-- <Button
            x:Name="CounterBtn"
            Text="Create Check2"
            SemanticProperties.Hint="Counts the number of times you click"
            Clicked="OnCounterClicked"
            VerticalOptions="Center"
            HorizontalOptions="Fill"/> -->
       </VerticalStackLayout>
</ContentPage>